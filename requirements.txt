# --- Core Framework ---
aiogram==3.13.1                # Telegram bot framework (v3 async)
SQLAlchemy==2.0.32              # ORM for DB
APScheduler==3.10.4             # Task scheduling (reminders)

# --- Database Drivers ---
asyncpg==0.29.0                 # Async PostgreSQL driver
aiosqlite==0.20.0               # For local dev/testing on SQLite
greenlet==3.1.1                 # SQLAlchemy async requires this

# --- Config & Validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1            # Local .env loading

# --- Utils ---
httpx==0.27.2                   # Async HTTP client (used by OpenAI)
dateparser==1.2.0               # Parse natural language dates ("завтра в 5")
pytz==2024.1                    # Timezone handling

# --- AI ---
openai==1.51.2                  # OpenAI API client (for GPT + Whisper)

# --- Optional (если используешь локально для отладки) ---
# ffmpeg-python==0.2.0           # Только если реально запускаешь локальный ffmpeg